# Django core dependencies
Django>=4.2

# Used in views.py
requests
openpyxl
# For math and datetime imports
# (math, datetime, os, csv, json, are standard library and do not need to be added)
# Django core dependencies
Django>=4.2

# Used in views.py
requests
openpyxl
django>=4.2
razorpay>=1.3.0
gunicorn>=21.2.0
